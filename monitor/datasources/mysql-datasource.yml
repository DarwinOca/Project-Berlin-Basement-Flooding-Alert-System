apiVersion: 1

datasources:
  - name: MySQL Database
    type: mysql
    uid: mysql-db-1
    isDefault: false
    database: ${DB_NAME}
    url: ${DB_HOST}:${DB_PORT}
    user: ${DB_USER}
    secureJsonData:
      password: ${DB_PASSWORD}
    version: 1